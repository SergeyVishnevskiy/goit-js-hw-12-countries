<div class="js-container">
    <ul class="countries-list">
        {{#each this}}
        <li class="countries-list__item">
            <img class="small-image" src="{{flag}}" alt="flag" width="100">
            <p>{{name}}</p>
        </li>
        {{/each}}
    </ul>
</div>